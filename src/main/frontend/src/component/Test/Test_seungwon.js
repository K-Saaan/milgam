function Header(){ 
    // 3. App() 안에 모든 코드를 넣지 않고, 코드의 일정 부분을 따로 뺄 수 있다.
    // 이 따로 뺀 코드를 담고 있는 함수가 사용자 정의 태그로 사용되는 함수이다.
    return (
      <header>
        <h1><a href="/">WEB</a></h1>
      </header>
    )
  }
  