// 혼잡도, 이상행동, 혼잡 추이, 혼잡 비율 공통 박스 스타일
const paperStyle = (theme) => ({
    flex: 1,
    padding: 2,
    bgcolor: theme.palette.background.paper,
    color: theme.palette.text.primary,
    height: '120px', // 높이를 120px로 설정
    borderRadius: 2,
  });
  
  export default paperStyle;
  